# LiveKit Agents framework — pinned to v1.5.x (adaptive interruption ships here)
# The [google,silero] extras pull in Gemini LLM support + Silero VAD for turn detection
livekit-agents[google,silero]>=1.5.6,<2.0

# Gradium STT + TTS (single package, both included)
livekit-plugins-gradium>=1.3.12

# ai-coustics noise cancellation for the "highway" / noisy-environment requirement
livekit-plugins-ai-coustics

# Tavily for the real-time external lookup (your demo "wow" moment)
tavily-python

# Env loading
python-dotenv

# yt-dlp for grabbing the voice-cloning sample from YouTube (one-time use, but cheap to keep)
yt-dlp